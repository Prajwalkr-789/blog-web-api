{
  "version": 2,
  "builds": [
    {
      "src": "/api/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "handle": "filesystem" }, // Serve static files
    {
      "src": "/(.*)", // Match all routes
      "dest": "/index.js" // Forward requests to index.js
    }
  ]
}
